# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.log
*.pem

# env files — never commit
.env
.env.local
# agent coordinator credentials (GitHub, Telegram, Langfuse) — .example only
.env.agents
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# python
__pycache__/
scripts/.myschools_cache/

# typescript
*.tsbuildinfo
next-env.d.ts

# school data is environment-specific, copied in at deploy time
data/schools.json

# local files
*.csv
install.cmd

# Sentry Config File
.env.sentry-build-plugin
build_output.log
scraper.log

# planning & strategy docs — keep private, never publish to the public repo
DEVLOG.md
ROADMAP.md
NEXT.md
NEXT_SKILL.md
TODO.md
Strategy_PRD.md
CompAnalysis.md
*.pdf

# Local personal tool — not part of the app
domaincheck.py

# Local Claude Code state (worktrees, local settings) — never commit
.claude/

# Private planning/strategy lives in the separate private Planning repo
planning-docs/

# Finder duplicate files
* copy*
.env*.local
